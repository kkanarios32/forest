\date{2025-03-25}
\import{base-macros}
\taxon{Definition}
\title{Huffman Code}
\p{Given #{x_1, \ldots x_m} proceed as follows:
\ol{
  \li{Take two #{x_i, x_j} with smallest #{p(x_i), p(x_j)} append 0 and 1 to their respective codes.}
  \li{Combine them and treat them as a new value with #{p(x_i') = p(x_i) + p(x_j)}}
  \li{Repeat}
}
}
